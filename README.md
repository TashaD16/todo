# Todo App - Современное приложение для управления задачами

Веб-приложение для управления задачами, построенное на современных технологиях.

## 🚀 Технологический стек

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - стилизация
- **IndexedDB** - локальная база данных
- **date-fns** - работа с датами
- **lucide-react** - иконки

## ✨ Функционал

### MVP (реализовано)
- ✅ Создание задач с заголовком, описанием, дедлайном, приоритетом и категорией
- ✅ Просмотр списка задач
- ✅ Отметка выполнения задач
- ✅ Редактирование задач
- ✅ Удаление задач (мягкое удаление)
- ✅ Фильтрация по статусу, приоритету
- ✅ Поиск по тексту
- ✅ Сортировка задач
- ✅ Статистика задач

### Планируется
- Экспорт/импорт задач (JSON, CSV)
- Уведомления о дедлайнах
- Темная тема
- Подзадачи
- Группировка по категориям

## 📦 Установка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

## 🗄️ База данных

Приложение использует **IndexedDB** для локального хранения данных в браузере. Данные сохраняются автоматически и не требуют настройки сервера.

### Структура данных

**Tasks (Задачи):**
- id, title, description
- created_at, updated_at, completed_at, deadline
- priority (low, medium, high)
- category, status (active, completed, deleted)
- is_deleted, deleted_at

**Categories (Категории):**
- id, name, color, created_at

## 📁 Структура проекта

```
src/
├── components/      # React компоненты
│   ├── TaskList.tsx
│   ├── TaskItem.tsx
│   ├── TaskForm.tsx
│   └── Filters.tsx
├── database/        # Работа с IndexedDB
│   └── db.ts
├── services/        # Бизнес-логика
│   ├── TaskService.ts
│   └── CategoryService.ts
├── types/           # TypeScript типы
│   └── index.ts
├── utils/           # Утилиты
│   └── dateUtils.ts
├── App.tsx
├── main.tsx
└── index.css
```

## 🎨 Особенности

- **Адаптивный дизайн** - работает на всех устройствах
- **Темная тема** - поддержка темной темы (в разработке)
- **Быстрая работа** - оптимизировано для производительности
- **Офлайн режим** - работает без интернета
- **Типобезопасность** - полная поддержка TypeScript

## 📝 Лицензия

MIT

